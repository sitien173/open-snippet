---
plan: docs/plans/2026-05-29-openmacro
updated_at: 2026-06-06T10:35:00Z
current_phase: 5
status: ACTIVE
owner: coordinator
session_refs:
  codex: 019e987e-f0c4-7190-abc8-d9fee9929703
  gemini: null
---
## next_action
Phase 5 (Settings UI, gemini front-side). Read PLAN.md Phase 5 section, draft phase-05/prompt.md, create phase-05/journal.md skeleton, then dispatch via `mcp__openmcp__run` backend="gemini" (or "agy"). Worker consumes `Snippet`/`VarDecl` shapes from `openmacro/src-tauri/src/store`.

## read_first
- docs/plans/2026-05-29-openmacro/PLAN.md
- docs/plans/2026-05-29-openmacro/phase-04/journal.md

## completed_tasks
- phase-1 / codex / squashed → a76dcee / Tauri 2 scaffold, tray + single-instance + autostart, 8 module stubs
- phase-2 / codex / squashed → 24a7926 / YAML loader, debounced watcher, AC matcher (buffer + boundary)
- phase-3 / codex / squashed → 985fdd9 / hook ring + foreground denylist + clipboard/SendInput injector + orchestrator pause + Notepad smoke scaffold
- phase-4 / codex / squashed → b59e4ef / {{date}}/{{time}}/{{datetime}}/{{clipboard}}/{{cursor}} resolvers, $|$ token (loader validation + UTF-16 caret math), orchestrator wired with NotifySink

## blockers
<empty>

## decisions_delta
- 2026-06-06: Phase 4 review accepted PASS_WITH_DEBT. Carry-forward debt:
  - `cargo-llvm-cov` not installed in worker env — ≥80% coverage gate deferred. Install + run before Phase 6 review.
  - `engine/orchestrator.rs:133` swallows `ResolveError::UnterminatedPlaceholder` silently (no notify, no buffer reset). MEDIUM finding — wire into `NotifySink` or distinct error path.
  - `expand::resolver::Resolved.cursor_chars_after_token` is named *chars* but holds UTF-16 code units — rename for clarity (LOW).

## uncommitted_files
<empty>
